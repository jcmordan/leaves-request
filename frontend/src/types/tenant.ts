export interface OrgInfoConfig {
  taxId?: string | null
  address?: string | null
  phoneNumber?: string | null
}

export interface PrintConfig {
  footerMessage?: string | null
  showLogo?: boolean | null
}
